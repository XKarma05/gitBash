git init
git add "..."
git add . (all)
git commit -m "first commit" (-m = message)

git status
git diff
git diff --staged
git log

git rm "..." remove from repository and folder

rename a file :
rename it
add the nex one
remove the former one
commit
